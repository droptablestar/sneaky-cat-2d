# Development Workflow

- Run the game from Godot editor (F5) or the play button
- Edit `.gd` scripts and `.tscn` files directly — Godot hot-reloads scripts
- F11 toggles fullscreen at runtime
- Batch related changes before committing — don't commit after every small fix
